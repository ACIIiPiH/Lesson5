1. Написать библиотеку для кэширования.
  1.1. Создание кеш таблицы (аргументы: имя таблиці)
  ok = my_cache:create(TableName).
  1.2. Добавить запись в кеш (аргументы: имя таблицы, ключ, значение,
  время жизни записи)
  ok = my_cache:insert(TableName, Key, Value, 600).

1.3. Прочитать значение по ключу (функция должна возвращать, только
  актуальные, т.е. НЕ устаревшие данные)
  {ok, Value} = my_cache:lookup(TableName, Key).

1.4. Очистить из памяти все устаревшие записи
ok = my_cache:delete_obsolete(TableName).


2. Провести сравнительный анализ всех способов хранения данных в
  режиме ключ/значение: maps, proplists, dict, process dictionary, ets.
  Тестируем все: скорость создания, обновления полей, чтения значения
  через сопоставление по образцу, и чтение через вызов функций
  timer:tc(fun() -> io:format(“~p”, []) end)
  timer:tc(M, F, A)
